version: 2

version: 2

sphinx:
  configuration: docs/source/conf.py

python:
  version: 3.9
  install:
    - requirements: docs/requirements.txt

build:
  image: latest

# Optional formats, leave empty or include pdf/epub if desired
formats: []
